properties([pipelineTriggers([githubPush()])])
pipeline {
    node('linux') {
        git url: 'https://github.com/Overwear/infrastructure_pipeline.git', branch: 'master'
        stage('Test') {
            sh "env"
        }
    }
}